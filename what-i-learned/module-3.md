2 different streaming methods

stream / astream.

both returns iterator for stream output. synchronous iterator for stream, async counterpart for astream.

one can use astream_events method in which he or she can subscribe to each token being generated by llm.
